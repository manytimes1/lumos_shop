<div class="col-12 col-sm-4 col-md-4">
    <h4 class="card-title">Add Role</h4>
    <form action="{{ url('create_new_role') }}" method="post">
        <div class="form-group">
            <label>Name</label>
            <input type="text" name="role[name]" required="required" placeholder="Name" class="form-control">
        </div>
        <button type="submit" class="btn btn-info btn btn-info waves-effect waves-light float-right">Save</button>
    </form>
</div>